# Leave empty for local Vite (proxy /api → http://localhost:4000)
# VITE_API_URL=

# Production / Render static site (set at build time, no trailing slash):
# VITE_API_URL=https://construction-erp-api.onrender.com
